import { Collapse } from "antd";
import { CollapseProps } from "antd";
import React from "react";

const OllamaFlowCollpase = (props: CollapseProps) => {
  return <Collapse {...props} />;
};

export default OllamaFlowCollpase;
