"use client";
import ConfigForm from "#/pages/config/ConfigForm";
import React from "react";

const Page = () => {
  return <ConfigForm onClose={() => {}} editConfig={undefined} />;
};

export default Page;
